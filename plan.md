1. **Update Categories in `js/state.js`:** Ensure the `CATEGORIES` array contains exactly these elements: Sleep, School/Work, Study, Exercise, Eat, Transport, Personal Care, Family Time, Hobbies, Entertainment, Social Media, Hanging Out, Chores, Competitions, Work.
2. **Update Analytics (`js/analytics.js`):** Implement the requested analytics pie chart using 24 hours (with unfilled time shown as 'free time'). Implement the persona classification based on the percentages of categories for a day.
3. **Implement Point System (`js/points.js`):** Add tasks system, the points logic. Include a bird that flaps and flies across the screen (CSS + JS animation). The bird awards 25 points on click, and task completion awards 50 points. Note: The codebase already has `awardPoints` in `state.js`, so maybe we augment it and add the UI in `points.js` and link it in `index.html`.
4. **Implement Currency System (`js/currency.js`):** Implement the green sapling growing mechanics (days 1-5 grow, 6-10 golden apples). Add watering device and functionality, awarding golden apples as currency. Add `currency.js` logic and link it in `index.html`.
